{
  "name": "@jlguenego/set",
  "version": "0.0.1",
  "description": "More operations on Sets: union, intersection, include, etc.",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "module": "build_esm/src/index.js",
  "private": false,
  "files": [
    "build/src",
    "build_esm/src"
  ],
  "license": "ISC",
  "keywords": [
    "set",
    "union",
    "intersection",
    "include"
  ],
  "scripts": {
    "test": "nyc mocha",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "compile:esm": "tsc --project tsconfig.esm.json",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint",
    "prerelease": "npm run compile && npm run compile:esm",
    "release": "standard-version"
  },
  "devDependencies": {
    "@types/mocha": "^8.2.0",
    "@types/node": "^14.11.2",
    "gts": "^3.0.3",
    "mocha": "^8.3.0",
    "nyc": "^15.1.0",
    "standard-version": "^9.1.0",
    "typescript": "^4.0.3"
  }
}
